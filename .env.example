# Base URL of the MMD backend API. For local dev this is the Express server
# (default port 9000). For production set it to the deployed backend URL.
NEXT_PUBLIC_API_URL=https://mmdbackend.onrender.com
